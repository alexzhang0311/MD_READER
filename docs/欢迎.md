# 欢迎使用 KYC 知识库

这是一个示例文档，用于展示 KYC 知识库的功能。

## 功能特性

- ✅ **文件浏览** — 根据目录结构展示 Markdown 文件列表
- ✅ **文件阅读** — 点击文件名即可渲染阅读 Markdown 内容
- ✅ **用户认证** — 支持登录、注册、退出
- ✅ **代码高亮** — 自动语法高亮显示代码块
- ✅ **搜索过滤** — 快速搜索文件名
- ✅ **响应式设计** — 支持桌面和移动端

## 代码示例

### Python

```python
def hello(name: str) -> str:
    """Say hello to someone."""
    return f"Hello, {name}!"

if __name__ == "__main__":
    print(hello("World"))
```

### JavaScript

```javascript
const greet = (name) => {
    console.log(`Hello, ${name}!`);
};

greet("Markdown Reader");
```

## 表格示例

| 功能 | 状态 | 说明 |
|------|------|------|
| 登录 | ✅ | 用户名密码登录 |
| 注册 | ✅ | 新用户注册 |
| 文件列表 | ✅ | 树形结构展示 |
| 文件阅读 | ✅ | Markdown 渲染 |
| 代码高亮 | ✅ | Pygments 语法高亮 |

## 引用

> Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档。
> —— John Gruber

## 图片

如果文档目录中有图片，可以使用标准 Markdown 语法引用：

```markdown
![图片描述](./image.png)
```

---

*此文件由 KYC 知识库自动生成*
